[{"id":1,"question":"What do you need for two functions to be the same type?","code":"","reference":"","choices":["They should share the same signatures, including parameter types and return types.","They should share the same parameter types but can return different types.","All functions should be the same type.","The functions should not be a first class type."],"answer":[0]},{"id":2,"question":"What does the `len()` function return if passed a UTF-8 encoded string?","code":"","reference":"","choices":["the number of characters","the number of bytes","It does not accept string types.","the number of code points"],"answer":[1]},{"id":3,"question":"Which is _not_ a valid loop construct in Go?","code":"Explanation: Go has only `for`-loops","reference":"Explanation: Go has only `for`-loops","choices":["`do { ... } while i < 5`","`for _,c := range \"hello\" { ... }`","`for i := 1; i < 5; i++ { ... }`","`for i < 5 { ... }`"],"answer":[0]},{"id":4,"question":"How will you add the number 3 to the right side?","code":"`values := []int{1, 1, 2}` Explanation: slices in GO are immutable, so calling `append` does not modify the slice","reference":"Explanation: slices in GO are immutable, so calling `append` does not modify the slice","choices":["`values.append(3)`","`values.insert(3, 3)`","`append(values, 3)`","`values = append(values, 3)`"],"answer":[3]},{"id":5,"question":"What is the value of `Read`?","code":"go const (   Write = iota   Read   Execute )","reference":"```","choices":["0","1","2","a random value"],"answer":[1]},{"id":6,"question":"Which is the _only_ valid import statement in Go?","code":"","reference":"","choices":["`import \"github/gin-gonic/gin\"`","`import \"https://github.com/gin-gonic/gin\"`","`import \"../template\"`","`import \"github.com/gin-gonic/gin\"`"],"answer":[3]},{"id":7,"question":"What would happen if you attempted to compile and run this Go program?","code":"go package main var GlobalFlag string func main() {   print(\"[\"+GlobalFlag+\"]\") }","reference":"```","choices":["It would not compile because `GlobalFlag` was never initialized.","It would compile and print `[]`.","It would compile and print nothing because `\"[\" +nil+\"]\"` is also `nil`.","It would compile but then panic because `GlobalFlag` was never initialized."],"answer":[1]},{"id":8,"question":"From where is the variable `myVar` accessible if it is declared outside of any functions in a file in package `myPackage` located inside module `myModule`?","code":"Explanation: to make the variable available outside of `myPackage` change the name to `MyVar`. See also an example of [Exported names](https://tour.golang.org/basics/3) in the Tour of Go.","reference":"See also an example of [Exported names](https://tour.golang.org/basics/3) in the Tour of Go.","choices":["It can be accessed anywhere inside `myPackage`, not the rest of myModule.","It can be accessed by any application that imports `myModule`.","It can be accessed from anywhere in `myModule`.","It can be accessed by other packages in `myModule` as long as they import `myPackage`"],"answer":[0]},{"id":9,"question":"How do you tell `go test` to print out the tests it is running?","code":"","reference":"","choices":["`go test`","`go test -x`","`go test --verbose`","`go test -v`"],"answer":[3]},{"id":10,"question":"This code printed `{0, 0}`. How can you fix it?","code":"go type Point struct {   x int   y int } func main() {   data := []byte(`{\"x\":1, \"y\": 2}`)   var p Point   if err := json.Unmarshal(data, &p); err != nil {     fmt.Println(\"error: \", err)   } else {     fmt.Println(p)   } }","reference":"```","choices":["use `json.Decoder`","Pass a pointer to `data`","Make `X` and `Y` exported (uppercase)","Use field tags"],"answer":[2]},{"id":11,"question":"What does a `sync.Mutex` block white it is locked?","code":"","reference":"","choices":["all goroutines","any other call to lock that `Mutex`","any reads or writes of the variable it is locking","any writes to the variable it is locking"],"answer":[1]},{"id":12,"question":"What is an idiomatic way to pause execution of the current scope until an arbitrary number of goroutines have returned?","code":"Explanation: this is exactly what `sync.WaitGroup` is designed for - [Use sync.WaitGroup in Golang](https://nanxiao.me/en/use-sync-waitgroup-in-golang/)","reference":"Explanation: this is exactly what `sync.WaitGroup` is designed for - [Use sync.WaitGroup in Golang](https://nanxiao.me/en/use-sync-waitgroup-in-golang/)","choices":["Pass an `int` and `Mutex` to each and count when they return.","Loop over a `select` statement.","Sleep for a safe amount of time.","`sync.WaitGroup`"],"answer":[3]},{"id":13,"question":"What is a side effect of using `time.After` in a `select` statement?","code":"Note: it doesn't block `select` and does not block other channels.","reference":"Note: it doesn't block `select` and does not block other channels.","choices":["It blocks the other channels.","It is meant to be used in select statements without side effects.","It blocks the `select` statement until the time has passed.","The goroutine does not end until the time passes."],"answer":[1]},{"id":14,"question":"What is the select statement used for?","code":"","reference":"","choices":["executing a function concurrently","executing a different case based on the type of a variable","executing a different case based on the value of a variable","executing a different case based on which channel returns first"],"answer":[3]},{"id":15,"question":"According to the Go documentation standard, how should you document this function?","code":"go func Add(a, b int) {   return a + b }  go // Calculate a + b // - a: int // - b: int // - returns: int func Add(a, b int) {   return a + b }  go // Does a + b func Add(a, b int) {   return a + b }  go // Add returns the sum of a and b func Add(a, b int) {   return a + b }  go // returns the sum of a and b func Add(a, b int) {   return a + b }  Explanation: documentation block should start with a function name","reference":"Explanation: documentation block should start with a function name","choices":["A","B","C","D"],"answer":[2]},{"id":16,"question":"What restriction is there on the type of `var` to compile this `i := myVal.(int)?`","code":"Explanation: This kind of type casting (using `.(type)`) is used on interfaces only.","reference":"Explanation: This kind of type casting (using `.(type)`) is used on interfaces only.","choices":["`myVal` must be an integer type, such as `int`, `int64`, `int32`, etc.","`myVal` must be able to be asserted as an `int`.","`myVal` must be an interface.","`myVal` must be a numeric type, such as `float64` or `int64`."],"answer":[2]},{"id":17,"question":"What is a channel?","code":"","reference":"","choices":["a global variable","a medium for sending values between goroutines","a dynamic array of values","a lightweight thread for concurrent programming"],"answer":[1]},{"id":18,"question":"How can you make a file build only on Windows?","code":"","reference":"","choices":["Check runtime.GOOS.","Add a // +build windows comment anywhere in the file.","Add a _ prefix to the file name.","Add a // +build windows comment at the top of the file."],"answer":[3]},{"id":19,"question":"What is the correct way to pass this as a body of an HTTP POST request?","code":"go data := \"A group of Owls is called a parliament\"","reference":"```","choices":["resp, err := http.Post(\"https://httpbin.org/post\", \"text/plain\", []byte(data))","resp, err := http.Post(\"https://httpbin.org/post\", \"text/plain\", data)","resp, err := http.Post(\"https://httpbin.org/post\", \"text/plain\", strings.NewReader(data))","resp, err := http.Post(\"https://httpbin.org/post\", \"text/plain\", &data)"],"answer":[2]},{"id":20,"question":"What should the idiomatic name be for an interface with a single method and the signature `Save() error`?","code":"","reference":"","choices":["Saveable","SaveInterface","ISave","Saver"],"answer":[3]},{"id":21,"question":"A `switch` statement **_** its own lexical block. Each `case` statement **_** an additional lexical block.","code":"Relevant excerpt from the article: > _The second if statement is nested inside the first, so a variable declared in the first if statement is visible to the second if statement. There are similar rules in switch: Each case has its own lexical block in addition to the conditional lexical block._","reference":"> _The second if statement is nested inside the first, so a variable declared in the first if statement is visible to the second if statement. There are similar rules in switch: Each case has its own lexical block in addition to the conditional lexical block._","choices":["does not create; creates","does not create; does not create","creates; creates","creates; does not create"],"answer":[2]},{"id":22,"question":"What is the default case sensitivity of the JSON `Unmarshal` function?","code":"Relevant excerpt from the article: > _To unmarshal JSON into a struct, Unmarshal matches incoming object keys to the keys used by Marshal (either the struct field name or its tag), preferring an exact match but also accepting a case-insensitive match. By default, object keys which don't have a corresponding struct field are ignored (see Decoder.DisallowUnknownFields for an alternative)._","reference":"> _To unmarshal JSON into a struct, Unmarshal matches incoming object keys to the keys used by Marshal (either the struct field name or its tag), preferring an exact match but also accepting a case-insensitive match. By default, object keys which don't have a corresponding struct field are ignored (see Decoder.DisallowUnknownFields for an alternative)._","choices":["The default behavior is case insensitive, but it can be overridden.","Fields are matched case sensitive.","Fields are matched case insensitive.","The default behavior is case sensitive, but it can be overridden."],"answer":[0]},{"id":23,"question":"What is the difference between the `time` package’s `Time.Sub()` and `Time.Add()` methods?","code":"","reference":"","choices":["Time.Add() is for performing addition while Time.Sub() is for nesting timestamps.","Time.Add() always returns a later time while time.Sub always returns an earlier time.","They are opposites. Time.Add(x) is the equivalent of Time.Sub(-x).","Time.Add() accepts a Duration parameter and returns a Time while Time.Sub() accepts a Time parameter and returns a Duration."],"answer":[3]},{"id":24,"question":"What is the risk of using multiple field tags in a single struct?","code":"","reference":"","choices":["Every field must have all tags to compile.","It tightly couples different layers of your application.","Any tags after the first are ignored.","Missing tags panic at runtime."],"answer":[1]},{"id":25,"question":"Where is the built-in recover method useful?","code":"Relevant excerpt from the article: > _Recover is useful only when called inside deferred functions. Executing a call to recover inside a deferred function stops the panicking sequence by restoring normal execution and retrieves the error message passed to the panic function call. If recover is called outside the deferred function, it will not stop a panicking sequence._","reference":"> _Recover is useful only when called inside deferred functions. Executing a call to recover inside a deferred function stops the panicking sequence by restoring normal execution and retrieves the error message passed to the panic function call. If recover is called outside the deferred function, it will not stop a panicking sequence._","choices":["in the main function","immediately after a line that might panic","inside a deferred function","at the beginning of a function that might panic"],"answer":[2]},{"id":26,"question":"Which choice does _not_ send output to standard error?","code":"","reference":"","choices":["println(message)","log.New(os.Stderr, \"\", 0).Println(message)","fmt.Errorf(\"%sn\", message)","fmt.Fprintln(os.Stderr, message)"],"answer":[0]},{"id":27,"question":"How can you tell Go to import a package from a different location?","code":"","reference":"","choices":["Use a proxy.","Change the import path.","Use a replace directive in go.mod.","Use a replace directory."],"answer":[2]},{"id":28,"question":"If your current working directory is the top level of your project, which command will run all its test packages?","code":"Relevant excerpt from the article: > _Relative patterns are also allowed, like \"go test ./...\" to test all subdirectories._","reference":"> _Relative patterns are also allowed, like \"go test ./...\" to test all subdirectories._","choices":["go test all","go run --all","go test .","go test ./..."],"answer":[3]},{"id":29,"question":"Which encodings can you put in a string?","code":"Relevant excerpt from the article: > _Package encoding defines an interface for character encodings, such as Shift JIS and Windows 1252, that can convert to and from UTF-8._","reference":"> _Package encoding defines an interface for character encodings, such as Shift JIS and Windows 1252, that can convert to and from UTF-8._","choices":["any, it accepts arbitary bytes","any Unicode format","UTF-8 or ASCII","UTF-8"],"answer":[3]},{"id":30,"question":"How is the behavior of `t.Fatal` different inside a `t.Run`?","code":"Explanation:   Fatal is equivalent to Log followed by FailNow.   Log formats its arguments using default formatting, analogous to Println, and records the text in the error log.   FailNow marks the function as having failed and stops its execution by calling runtime.Goexit (which then runs all deferred calls in the current goroutine). Execution will continue at the next test or benchmark. FailNow must be called from the goroutine running the test or benchmark function, not from other goroutines created during the test. Calling FailNow does not stop those other goroutines.   Run runs f as a subtest of t called name. It runs f in a separate goroutine and blocks until f returns or calls t.Parallel to become a parallel test. Run reports whether f succeeded (or at least did not fail before calling t.Parallel).   Run may be called simultaneously from multiple goroutines, but all such calls must return before the outer test function for t returns.","reference":"Run may be called simultaneously from multiple goroutines, but all such calls must return before the outer test function for t returns.","choices":["There is no difference.","t.Fatal does not crash the test harness, preserving output messages.","t.Fatal stops execution of the subtest and continues with other test cases.","t.Fatal stops all tests and contains extra information about the failed subtest."],"answer":[2]},{"id":31,"question":"What does `log.Fatal` do?","code":"Relevant excerpt from the article: > _Fatal is equivalent to Print() followed by a call to os.Exit(1)._","reference":"> _Fatal is equivalent to Print() followed by a call to os.Exit(1)._","choices":["It raises a panic.","It prints the log and then raises a panic.","It prints the log and then safely exits the program.","It exits the program."],"answer":[2]},{"id":32,"question":"Which is a valid Go time format literal?","code":"Relevant excerpt from the article: > _Most programs can use one of the defined constants as the layout passed to Format or Parse. The rest of this comment can be ignored unless you are creating a custom layout string._","reference":"> _Most programs can use one of the defined constants as the layout passed to Format or Parse. The rest of this comment can be ignored unless you are creating a custom layout string._","choices":["\"2006-01-02\"","\"YYYY-mm-dd\"","\"y-mo-d\"","\"year-month-day\""],"answer":[0]},{"id":33,"question":"How should you log an error (err)","code":"Explanation: There is defined neither log.ERROR, nor log.Error() in [log package](https://pkg.go.dev/log); log.Print() arguments are handled in the manner of fmt.Print(); log.Printf() arguments are handled in the manner of fmt.Printf().","reference":"Explanation: There is defined neither log.ERROR, nor log.Error() in [log package](https://pkg.go.dev/log); log.Print() arguments are handled in the manner of fmt.Print(); log.Printf() arguments are handled in the manner of fmt.Printf().","choices":["`log.Error(err)`","`log.Printf(\"error: %v\", err)`","`log.Printf(log.ERROR, err)`","`log.Print(\"error: %v\", err)`"],"answer":[1]},{"id":34,"question":"How does a `sync.Mutex` block while it is locked?","code":"","reference":"","choices":["any other call to lock that Mutex","all goroutines","any writes to the variable it is locking","any reads or writes of the variable is it locking"],"answer":[0]},{"id":35,"question":"Which file names will the `go test` command recognize as test files?","code":"","reference":"","choices":["any that starts with `test`","any files that include the word `test`","only files in the root directory that end in `_test.go`","any that ends in `_test.go`"],"answer":[3]},{"id":36,"question":"What will be the output of this code?","code":"ch := make(chan int) ch <- 7 val := <-ch fmt.Println(val)","reference":"```","choices":["0","It will deadlock","It will not compile","2.718"],"answer":[1]},{"id":37,"question":"What will be the output of this program?","code":"ch := make(chan int) close(ch) val := <-ch fmt.Println(val)","reference":"```","choices":["It will deadlock","It will panic","0","NaN"],"answer":[2]},{"id":38,"question":"What will be printed in this code?","code":"var stocks map[string]float64 // stock -> price price := stocks[\"MSFT\"] fmt.Println(\"%fn\", price)","reference":"```","choices":["0","0.000000","The code will panic","NaN"],"answer":[0]},{"id":39,"question":"What is the common way to have several executables in your project?","code":"1. [stackoverflow](https://stackoverflow.com/questions/9985559/organizing-a-multiple-file-go-project/51942843#51942843) 2. [medium](https://medium.com/@benbjohnson/structuring-applications-in-go-3b04be4ff091) 3. [medium](https://medium.com/golang-learn/go-project-layout-e5213cdcfaa2)","reference":"3. [medium](https://medium.com/golang-learn/go-project-layout-e5213cdcfaa2)","choices":["Have a cmd directory and a directory per executable inside it.","Comment out main.","Use build tags.","Have a pkg directory and a directory per executable inside it."],"answer":[0]},{"id":40,"question":"How can you compile main.go to an executable that will run on OSX arm64 ?","code":"","reference":"","choices":["Set GOOS to **arm64** and GOARCH to **darwin**.","Set GOOS to **osx** and GOARCH to **arm64**.","Set GOOS to **arm64** and GOARCH to **osx**.","Set GOOS to **darwin** and GOARCH to **arm64**."],"answer":[3]}]